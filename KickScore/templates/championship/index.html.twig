{% extends 'base.html.twig' %}

{% block title %}Création - KickScore{% endblock %}

{% block body %}
    <div class="h-[calc(100vh-2rem)]">
        <div class="h-full flex items-center justify-center bg-gradient-to-br from-slate-50 to-white rounded-xl p-8">
            <div class="w-full max-w-md">
                <h1 class="text-4xl font-bold text-slate-800 text-center mb-8">
                    Création
                    <span class="block mt-2 text-blue-900 text-2xl font-medium" id="subtitle">
                        d'un nouveau championnat
                    </span>
                </h1>

                {% for message in app.flashes('success') %}
                    <div class="bg-green-50 border border-green-200 text-green-800 rounded-lg p-4 mb-6">
                        <p class="text-sm font-medium">{{ message }}</p>
                    </div>
                {% endfor %}
                {% for message in app.flashes('error') %}
                    <div class="bg-red-50 border border-red-200 text-red-800 rounded-lg p-4 mb-6">
                        <p class="text-sm font-medium">{{ message }}</p>
                    </div>
                {% endfor %}

                <!-- tabs -->
                <nav class="flex border-b border-slate-200 mb-6">
                    <button id="tab_creation"
                            class="px-6 py-3 text-sm font-medium border-b-2 border-transparent hover:border-slate-300 hover:text-slate-600 transition-colors relative -mb-[2px]">
                        Création classique
                    </button>
                    <button id="tab_tournament"
                            class="px-6 py-3 text-sm font-medium border-b-2 border-transparent hover:border-slate-300 hover:text-slate-600 transition-colors relative -mb-[2px]">
                        Générer un tournoi
                    </button>
                </nav>

                <!-- create classic -->
                <div id="panel_creation" class="hidden">
                    <div class="space-y-6">
                        <div class="bg-white rounded-lg shadow-sm border border-slate-100 p-6">
                            <div class="flex flex-col space-y-4">
                                <a href="#" id="btn_manual"
                                   class="w-full py-3 px-4 rounded-lg text-slate-700 bg-slate-50 hover:bg-slate-100 transition-colors font-medium text-center">
                                    Créer manuellement
                                </a>
                                <a href="#" id="btn_import"
                                   class="w-full py-3 px-4 rounded-lg text-slate-700 bg-slate-50 hover:bg-slate-100 transition-colors font-medium text-center">
                                    Importer depuis un fichier
                                </a>
                            </div>
                        </div>

                        <!-- classic -->
                        <form id="championship_manual" method="post" action="{{ path('app_championship_create') }}"
                              class="hidden space-y-6 bg-white rounded-lg shadow-sm border border-slate-100 p-6">
                            <input type="hidden" name="addType" value="CHAMPIONSHIP">
                            <div class="space-y-1">
                                <label for="name" class="text-sm font-medium text-slate-700">Nom</label>
                                <input type="text" id="name" name="name"
                                       placeholder="Nom du championnat"
                                       class="w-full px-4 py-3 rounded-lg border border-slate-200 focus:ring-2 focus:ring-blue-900 focus:border-blue-900 transition-colors">
                            </div>
                            <div class="space-y-1">
                                <label for="dateInput" class="text-sm font-medium text-slate-700">Date de début</label>
                                <input type="datetime-local" id="dateInput" name="date_start"
                                       class="w-full px-4 py-3 rounded-lg border border-slate-200 focus:ring-2 focus:ring-blue-900 focus:border-blue-900 transition-colors">
                                <p id="date_valid" class="hidden text-sm text-green-600 mt-1"></p>
                                <label for="dateInput" class="text-sm font-medium text-slate-700">Date de fin</label>
                                <input type="datetime-local" id="dateInput" name="date_end"
                                       class="w-full px-4 py-3 rounded-lg border border-slate-200 focus:ring-2 focus:ring-blue-900 focus:border-blue-900 transition-colors">
                                <p id="date_valid" class="hidden text-sm text-green-600 mt-1"></p>
                            </div>
                            <button type="submit"
                                    class="w-full px-8 py-4 bg-blue-900 text-white rounded-lg hover:bg-blue-800 transition-colors font-medium text-lg shadow-sm">
                                Créer le championnat
                            </button>
                        </form>

                        <!-- import -->
                        <form id="championship_import" method="post" action="{{ path('app_championship_import') }}"
                              class="hidden space-y-6 bg-white rounded-lg shadow-sm border border-slate-100 p-6"
                              enctype="multipart/form-data">
                            <div class="space-y-1">
                                <label for="file" class="text-sm font-medium text-slate-700">Fichier à importer</label>
                                <input type="file" id="file" name="file" required
                                       class="w-full px-4 py-3 rounded-lg border border-slate-200 focus:ring-2 focus:ring-blue-900 focus:border-blue-900 transition-colors">
                            </div>
                            <button type="submit"
                                    class="w-full px-8 py-4 bg-blue-900 text-white rounded-lg hover:bg-blue-800 transition-colors font-medium text-lg shadow-sm">
                                Importer le championnat
                            </button>
                        </form>
                    </div>
                </div>

                <!-- tournament -->
                <div id="panel_tournament" class="hidden">
                    <form method="post" action="{{ path('app_generate_tournament', {'id': 'CHAMPIONSHIP_ID'}) }}"
                          class="space-y-6 bg-white rounded-lg shadow-sm border border-slate-100 p-6">
                        <div class="space-y-1">
                            <label for="championship" class="text-sm font-medium text-slate-700">Championnat source</label>
                            <select id="championship" name="championship_id"
                                    class="w-full px-4 py-3 rounded-lg border border-slate-200 focus:ring-2 focus:ring-blue-900 focus:border-blue-900 transition-colors">
                                <option disabled selected value=""> -- Sélectionner un championnat terminé -- </option>
                                {% for championship in finishedChampionships %}
                                    <option value="{{ championship.id }}">{{ championship.name }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        <div class="space-y-1">
                            <label for="tournament_name" class="text-sm font-medium text-slate-700">Nom du tournoi</label>
                            <input type="text" id="tournament_name" name="tournament_name"
                                   placeholder="Nom du tournoi"
                                   class="w-full px-4 py-3 rounded-lg border border-slate-200 focus:ring-2 focus:ring-blue-900 focus:border-blue-900 transition-colors">
                        </div>
                        <div class="pt-4 border-t border-slate-200">
                            <label class="text-sm font-medium text-slate-700 block mb-3">Options du tournoi</label>
                            <div class="space-y-3">
                                <label class="flex items-center gap-3">
                                    <input type="checkbox" name="third_place_match" class="rounded border-slate-300 text-blue-900 focus:ring-blue-900">
                                    <span class="text-sm text-slate-600">Match pour la 3ème place</span>
                                </label>
                            </div>
                        </div>
                        <button type="submit"
                                class="w-full px-8 py-4 bg-blue-900 text-white rounded-lg hover:bg-blue-800 transition-colors font-medium text-lg shadow-sm">
                            Générer le tournoi
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        const tabs = {
            creation: document.getElementById('tab_creation'),
            tournament: document.getElementById('tab_tournament')
        };

        const panels = {
            creation: document.getElementById('panel_creation'),
            tournament: document.getElementById('panel_tournament')
        };

        const subtitle = document.getElementById('subtitle');

        function activateTab(tabId) {
            Object.values(tabs).forEach(tab => {
                tab.classList.remove('text-blue-900', 'border-blue-900');
                tab.classList.add('text-slate-500', 'border-transparent');
            });
            tabs[tabId].classList.add('text-blue-900', 'border-blue-900');
            tabs[tabId].classList.remove('text-slate-500', 'border-transparent');

            Object.values(panels).forEach(panel => panel.classList.add('hidden'));
            panels[tabId].classList.remove('hidden');

            subtitle.textContent = tabId === 'creation'
                ? "d'un nouveau championnat"
                : "d'un nouveau tournoi";
        }

        activateTab('creation');

        tabs.creation.addEventListener('click', () => activateTab('creation'));
        tabs.tournament.addEventListener('click', () => activateTab('tournament'));

        const btnManual = document.getElementById('btn_manual');
        const btnImport = document.getElementById('btn_import');
        const formManual = document.getElementById('championship_manual');
        const formImport = document.getElementById('championship_import');

        btnManual.addEventListener('click', (e) => {
            e.preventDefault();
            formManual.classList.remove('hidden');
            formImport.classList.add('hidden');

            btnManual.classList.add('bg-blue-900', 'text-white');
            btnManual.classList.remove('bg-slate-50', 'text-slate-700');

            btnImport.classList.remove('bg-blue-900', 'text-white');
            btnImport.classList.add('bg-slate-50', 'text-slate-700');
        });

        btnImport.addEventListener('click', (e) => {
            e.preventDefault();
            formImport.classList.remove('hidden');
            formManual.classList.add('hidden');

            btnImport.classList.add('bg-blue-900', 'text-white');
            btnImport.classList.remove('bg-slate-50', 'text-slate-700');

            btnManual.classList.remove('bg-blue-900', 'text-white');
            btnManual.classList.add('bg-slate-50', 'text-slate-700');
        });

        document.querySelector('#panel_tournament form').addEventListener('submit', function (event) {
            const select = document.getElementById('championship');
            const selectedId = select.options[select.selectedIndex].value;
            this.action = this.action.replace('CHAMPIONSHIP_ID', selectedId);
        });

        btnManual.click();
    </script>
{% endblock %}