{% extends 'base.html.twig' %}

{% block title %}Confirmation de suppression - KickScore{% endblock %}

{% block body %}
    <div class="h-[calc(100vh-2rem)]">
        <div class="h-full bg-gradient-to-br from-slate-50 to-white rounded-xl p-8">
            <div class="max-w-3xl mx-auto">
                <div class="mb-8 flex items-center justify-between">
                    <h1 class="text-3xl font-bold text-slate-800">
                        <span>{{'cannot_delete_user' | trans}}</span>
                        <span class="block mt-1 text-lg text-slate-600 font-normal">
                            {{'user_in_team' | trans}}
                        </span>
                    </h1>

                    <div class="space-x-3">
                        <a href="{{ path('app_user_index') }}"
                           class="px-4 py-2 bg-slate-100 text-slate-700 rounded-lg hover:bg-slate-200 transition-colors font-medium">
                            <span>{{'return_to_list' | trans}}</span>
                        </a>
                    </div>
                </div>

                <div class="bg-white rounded-lg shadow-sm border border-slate-200 divide-y divide-slate-200">
                    <div class="p-6">
                        <div class="rounded-lg bg-red-50 border border-red-200 p-4 mb-6">
                            <div class="flex items-start">
                                <div class="flex-shrink-0">
                                    <svg class="h-5 w-5 text-red-700" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd" />
                                    </svg>
                                </div>
                                <div class="ml-3">
                                    <h3 class="text-md font-medium text-red-800">
                                        Action impossible
                                    </h3>
                                    <div class="mt-2 text-sm text-red-700">
                                        <p>
                                            <span>{{'user_management.table.user' | trans}}</span> {{ user.FirstName }} {{ user.name }} <span>{{'user_delete_error' | trans}}</span>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <h2 class="text-xl font-semibold text-slate-800 mb-4">{{'team_details' | trans}}</span></h2>
                        <div class="space-y-6">
                            <div>
                                <p class="text-sm font-medium text-slate-500"><span>{{'team_name' | trans}} </span></p>
                                <p class="mt-1 text-slate-800">{{ user.team.name }}</p>
                            </div>

                            <div>
                                <p class="text-sm font-medium text-slate-500 mb-2"><span>{{'team_members' | trans}} </span></p>
                                <div class="bg-slate-50 rounded-lg divide-y divide-slate-200">
                                    {% for teammate in user.member.team.members %}
                                        <div class="p-4 flex items-center justify-between">
                                            <div>
                                                <p class="text-sm font-medium text-slate-700">
                                                    {{ teammate.fname }} {{ teammate.name }}
                                                </p>
                                                <p class="text-sm text-slate-500">{{ teammate.email }}</p>
                                            </div>
                                            {% if teammate.id == user.id %}
                                                <span class="px-3 py-1 text-xs font-medium rounded-full bg-amber-100 text-amber-800">
                                                    <span>{{'concerned_member' | trans}} </span>
                                                </span>
                                            {% endif %}
                                        </div>
                                    {% endfor %}
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="p-6 bg-slate-50 rounded-b-lg flex items-center justify-between">
                        <div class="flex items-center space-x-3">
                            <a href="{{ path('app_user_edit', {'id': user.id}) }}"
                               class="px-4 py-2 bg-red-800 text-white rounded-lg hover:bg-red-900 transition-colors font-medium shadow-sm">
                                Supprimer l'équipe
                            </a>
                            <a href="{{ path('app_team_edit', {'id': user.member.team.id}) }}"
                               class="px-4 py-2 bg-slate-100 text-slate-700 rounded-lg hover:bg-slate-200 transition-colors font-medium">
                                Annuler
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}